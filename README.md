Electric charging stations project

In order to run that project locally, you have to run composer install.

In order to see Api documentation, run localhost/api/documentation( it is done with swagger)
